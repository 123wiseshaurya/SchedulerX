# ===========================================
# JobScheduler Pro Configuration
# ===========================================

# Email Configuration (Required for email jobs)
# For Gmail: Use App Password (not regular password)
# 1. Enable 2FA in Google Account
# 2. Generate App Password: Account → Security → App Passwords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-16-character-app-password
EMAIL_ENABLED=true

# MinIO Configuration (File Storage)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Database Configuration (Optional - defaults to H2 in-memory)
# Uncomment below for PostgreSQL
# DB_URL=jdbc:postgresql://localhost:5432/jobscheduler
# DB_USERNAME=jobscheduler
# DB_PASSWORD=password

# Application Settings
APP_NAME=JobScheduler Pro
APP_VERSION=1.0.0

# Logging Level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# ===========================================
# Email Provider Examples
# ===========================================

# Gmail
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587

# Outlook/Hotmail
# MAIL_HOST=smtp-mail.outlook.com
# MAIL_PORT=587

# Yahoo Mail
# MAIL_HOST=smtp.mail.yahoo.com
# MAIL_PORT=587

# Custom SMTP
# MAIL_HOST=your-smtp-server.com
# MAIL_PORT=587